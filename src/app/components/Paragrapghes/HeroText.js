import styles from "./HeroText.module.css"

const HeroText = () => {
	return (
		<div className={styles.text_hero}>
			<h1 className={styles.heading}>HELLO;</h1>
			<p>im Max wacky creator that creates wacky things</p>
		</div>
	);
};

export default HeroText;
